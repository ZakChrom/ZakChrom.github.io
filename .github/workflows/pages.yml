name: GitHub Pages

on:
  push:
    branches:
      - master
  pull_request:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: true

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Get rust
        uses: actions-rs/toolchain@v1
        with:
          toolchain: nightly
          override: true
          profile: minimal
          target: ${{ matrix.target }}

      - name: Wasm thing
        run: rustup target add wasm32-unknown-unknown

      - name: Get trunk
        run: |
          wget https://github.com/thedodd/trunk/releases/download/v0.17.5/trunk-x86_64-unknown-linux-gnu.tar.gz
          tar -xf trunk-x86_64-unknown-linux-gnu.tar.gz

      - name: Build the site
        run: |
          ./trunk build
          mkdir dist/.well-known
          touch dist/.well-known/discord
          echo "dh=f6ff191e4a5bc5f7c6015cb294b104054eaf1d2f" > dist/.well-known/discord

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v1
        with:
          path: ./dist

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v1
